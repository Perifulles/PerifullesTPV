info:
  name: AuthMe
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8000/api/auth/me
  headers:
    - name: Authorization
      value: Bearer 1|Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
