info:
  name: GetUsers
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:8000/api/users
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
